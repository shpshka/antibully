# Datasets & outputs — never commit data (NTU's release agreement forbids redistribution)
data/*
!data/README.md
outputs/
runs/
weights/
runtime_data/
monitor.stdout.log
monitor.stderr.log
.env.runtime
*.pt
*.pth
*.onnx
# dataset archives (e.g. ut_interaction.zip) — far too large for git
*.zip

# local test footage — real school CCTV of identifiable minors; NEVER commit
test-data/
*.mp4
*.avi

# Local agent instructions — intentionally kept out of the repo
CLAUDE.md
CLAUDE.local.md
.claude/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Notebooks
.ipynb_checkpoints/

# IDE / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
